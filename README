//---- Cleancito

Custom solution to clean ads and stuff from subtitles.

Got tired of the alternatives. So much code and complexity. What's going on?

That's why I built this.

To compress the binary on Windows:
`upx --best --lzma target/release/min-sized-rust`